{
  "name": "@devmigue/fingerprint-wor",
  "version": "1.0.1",
  "type": "module",
  "description": "package to generate an unique fingerprint based on browser configuration.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src -d ./lib",
    "prepublishOnly": "npm run build"
  },
  "author": "devMigue",
  "license": "MIT",
  "dependencies": {
    "crypto-js": "^4.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
